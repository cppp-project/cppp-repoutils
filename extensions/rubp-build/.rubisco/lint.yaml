name: ✔️ Lint

id: lint

steps:
  - glob: "rubp_build/**/*.py"
    save-to: src-files
    recursive: True

  - name: 🛠️ Analysing the code with ruff
    run: ruff check rubp_build --config ruff.toml

  - name: 🛠️ Analysing the code with pylint
    run: [python, -m, pylint, -j, "${{ nproc }}", "${{ src-files }}"]

  - name: 🛠️ Running type check
    run: python -m pyright --verbose --threads ${{ nproc }} --level=warning rubp_build
